import type { Metadata } from 'next'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'Spam Detection - AI-Powered Text Classification',
  description: 'Detect spam messages using machine learning. Fast, accurate, and easy to use.',
  keywords: ['spam detection', 'machine learning', 'text classification', 'AI', 'NLP'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col antialiased">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
